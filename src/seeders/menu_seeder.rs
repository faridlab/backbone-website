//! Menu seeder
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Seeds initial Menu data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// MENU SEEDER
// ============================================================================

/// Seeder for menu
pub struct SeedMenuSeeder;

impl SeedMenuSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedMenuSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedMenuSeeder {
    fn name(&self) -> &'static str {
        "SeedMenuSeeder"
    }

    fn order(&self) -> i32 {
        11
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if website.menus table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM website.menus")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/menu_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM website.menus")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

