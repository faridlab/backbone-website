//! Database seeders for the website module
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This module provides seed data for initial database setup.
//! Seeders can be run via the seeder binary or CLI.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

mod contact_message_seeder;
mod menu_seeder;
mod menu_block_seeder;
mod page_seeder;
mod page_block_seeder;
mod redirect_seeder;
mod visitor_seeder;
mod visitor_track_seeder;
mod website_seeder;
mod website_audit_log_seeder;
mod website_member_seeder;

pub use contact_message_seeder::SeedContactMessageSeeder;
pub use menu_seeder::SeedMenuSeeder;
pub use menu_block_seeder::SeedMenuBlockSeeder;
pub use page_seeder::SeedPageSeeder;
pub use page_block_seeder::SeedPageBlockSeeder;
pub use redirect_seeder::SeedRedirectSeeder;
pub use visitor_seeder::SeedVisitorSeeder;
pub use visitor_track_seeder::SeedVisitorTrackSeeder;
pub use website_seeder::SeedWebsiteSeeder;
pub use website_audit_log_seeder::SeedWebsiteAuditLogSeeder;
pub use website_member_seeder::SeedWebsiteMemberSeeder;

// ============================================================================
// SEEDER TRAIT
// ============================================================================

/// Trait for database seeders
#[async_trait]
pub trait Seeder {
    /// Get the name of this seeder
    fn name(&self) -> &'static str;

    /// Get the order in which this seeder should run
    fn order(&self) -> i32;

    /// Check if this seeder should run (e.g., check if data already exists)
    async fn should_run(&self, pool: &PgPool) -> Result<bool>;

    /// Run the seeder
    async fn run(&self, pool: &PgPool) -> Result<()>;

    /// Rollback the seeder (optional)
    async fn rollback(&self, _pool: &PgPool) -> Result<()> {
        Ok(())
    }
}

// ============================================================================
// SEEDER TYPE
// ============================================================================

/// Type of seeder
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum SeederType {
    /// Reference data (required for application to function)
    Reference,
    /// Initial production data
    Production,
    /// Development/testing data
    Development,
    /// Test data (for automated tests)
    Test,
}

// ============================================================================
// CUSTOM SEEDERS
// ============================================================================

// <<< CUSTOM SEEDERS START >>>
// Add custom seeders here
// <<< CUSTOM SEEDERS END >>>

