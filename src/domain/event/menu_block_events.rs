//! Domain events for MenuBlock entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::MenuBlock;

/// Domain events for MenuBlock — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<MenuBlock>)` variant.
pub type MenuBlockEvent = CrudEvent<MenuBlock>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for MenuBlock events.
pub type MenuBlockEventPublisher = Arc<dyn CrudEventPublisher<MenuBlock> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopMenuBlockEventPublisher = NoOpCrudEventPublisher<MenuBlock>;
